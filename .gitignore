# Model weights are large and must not be committed.
*.gguf
*.gguf.*
models/

# Local build and editor state.
build/
.vscode/
.DS_Store

# Credentials, secrets, and runtime caches.
credentials/
credentials.*
secrets/
*.secret
*.pem
*.key
.cache/
cache/
tmp/
temp/
*.log
